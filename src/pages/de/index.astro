---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Hero from '../../components/Hero.astro';
import ProductCard from '../../components/product/ProductCard.astro';
import { useTranslations } from '../../utils/i18n';

const t = useTranslations('de');
---

<BaseLayout title={t.seo.title} description={t.seo.description}>
  <Hero locale="de" />
  
  <section class="py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">{t.product.title}</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <ProductCard
          title={t.product.resonators}
          description="HC-49S/SMD Quarzschwinger, Frequenzbereich 3,2MHz~100MHz"
          href="/de/products/resonators"
        />
        <ProductCard
          title={t.product.oscillators}
          description="Quarzinoszillatoren mit Präzision bis zu ±10ppm"
          href="/de/products/oscillators"
        />
        <ProductCard
          title={t.product.rtc}
          description="RTC-Module mit integriertem Quarz und Kompensationsschaltung"
          href="/de/products/rtc"
        />
      </div>
    </div>
  </section>
</BaseLayout>
